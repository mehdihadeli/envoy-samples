CONSUL_PATH="http://consul-server-bootstrap:8500" #we should use consul container address because and we can't use localhost because we should access consul container address in another container, here xds-server container
CONSUL_PREFIX=xDS